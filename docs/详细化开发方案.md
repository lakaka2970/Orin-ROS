# FT Radar ROS2 感知框架 — 完整开发方案

> 基于新版架构图（`60_XD_ROS_Architecture_20260609.drawio`）及 `FT_radar_dataset_requirement.md` 数据规范，全面整合所有新增需求与数据定义。

---

## 一、系统概述

### 1.1 运行环境

| 项目 | 规格 |
|------|------|
| 硬件平台 | NVIDIA Jetson AGX Orin 64GB |
| 操作系统 | Ubuntu 20.04 |
| Jetpack | 5.1.2 |
| ROS2 发行版 | Humble |
| 雷达 | Infineon CTRX8188F (16T16R), GMSL/FPD-Link, MAX929x |
| 摄像头 | Rmoncam A2 1080P USB UVC, MJPEG 1920×1080@30fps |
| 开发规范 | ROS2 src guideline |

### 1.2 上电顺序

**Radar 和 Camera 先上电 → 再启动 Orin 计算平台。**

### 1.3 硬件设备初始化

#### 1.3.1 安装 udev 持久化命名规则 (首次部署)

```bash
sudo cp scripts/99-ft-sensors.rules /etc/udev/rules.d/
sudo udevadm control --reload-rules && sudo udevadm trigger --subsystem-match=video4linux
ls -la /dev/camera_capture /dev/radar_ctrx0 /dev/radar_ctrx1
```

#### 1.3.2 启动雷达硬件

```bash
# 1. 启动雷达解串板 (加载 GMSL 驱动 + 配置 MAX929x 解串器)
cd ~/Desktop/Orin-ROS/src/integration-carkit88c0-gmsl
sudo python init_jetson.py

# 2. (可选) CTRX 雷达参数配置 + 启动连续 chirp 发射
cd ~/Desktop/Orin-ROS/src/integration-carkit88c0-gmsl/src/demo_app/boards/carkit_88c0/example/bin
sudo ./carkit88c0_gmsl_example

# 3. 采集雷达原始数据 (生成 scripts/output/ctrx0_raw.bin + ctrx1_raw.bin)
#    供 adc_rx.py / adc_rx_analog.cpp 离线回放使用
cd ~/Desktop/Orin-ROS/src/integration-carkit88c0-gmsl/scripts
bash capture_video0_2048x1024.sh
```

雷达数据规格:
- 设备: `/dev/video0` (ctrx0) + `/dev/video1` (ctrx1), udev 后: `/dev/radar_ctrx0` + `/dev/radar_ctrx1`
- 分辨率: 8192×1024, 像素格式: RG12 (12-bit padded to 16-bit)
- 帧率: 15.38 Hz, 数据量: 16 MiB/半集/帧, 合并后 32 MiB/帧
- 数据消费: `.bin` 文件由 `adc_rx.py` / `adc_rx_analog.cpp` 离线回放;
  实时模式由 `adc_rx.cpp` 直接 V4L2 mmap 采集（跳过 .bin 文件）
- 详细数据流参见 §1.4

#### 1.3.3 USB 摄像头

- 设备: `/dev/camera_capture` (udev 符号链接 → 实际 `/dev/video2`)
- 型号: Rmoncam A2 1080P, 驱动: `uvcvideo`
- 即插即用, 无需额外初始化

### 1.4 GMSL 雷达 ADC 数据流详解（知识补充）

> 本节详细说明雷达 ADC 原始数据从 CTRX 芯片到 `.bin` 文件的完整链路，用于了解解串板配置和数据采集机制。

#### 1.4.1 硬件数据链路

```
┌──────────────────┐     MIPI D-PHY      ┌─────────────┐   同轴线    ┌──────────────┐
│ CTRX8188F (CTRX0)│═════════════════════>│ MAX96717     │══════════>│              │
│ 8T8R, 4 RX        │  4-lane, 1000Mbps  │ (Serializer) │  GMSL Link │              │
│ ADC 25MSps, 12bit │   RAW12 + HSync    │ I2C: 0x84    │    A       │              │
└──────────────────┘                     └─────────────┘           │              │
                                                                    │ MAX9296B/A   │  MIPI CSI-2
┌──────────────────┐     MIPI D-PHY      ┌─────────────┐   同轴线    │ (Deserializer)│══════════>
│ CTRX8188F (CTRX1)│═════════════════════>│ MAX96717     │══════════>│ I2C: 0x90    │  2×4-lane
│ 8T8R, 4 RX        │  4-lane, 1000Mbps  │ (Serializer) │  GMSL Link │ on CARKIT88C0│  1200Mbps
│ ADC 25MSps, 12bit │   RAW12 + HSync    │ I2C: 0x84    │    B       │ carrier board│
└──────────────────┘                     └─────────────┘           └──────┬───────┘
                                                                          │
                                                            ┌─────────────┘
                                                            │ NVIDIA Orin AGX
                                                            ▼
                                                   ┌──────────────────┐
                                                   │ Tegra VI          │
                                                   │ (Video Input)     │
                                                   │ DMA → kernel buf  │
                                                   └──────┬───────────┘
                                                          │ V4L2
                                          ┌───────────────┼───────────────┐
                                          │               │               │
                                          ▼               ▼               │
                                   /dev/video0     /dev/video1           │
                                   (ctrx0, RX0-3)  (ctrx1, RX4-7)       │
                                   udev→            udev→                │
                                   radar_ctrx0      radar_ctrx1          │
                                          │               │               │
                                          └───────┬───────┘               │
                                                  │                       │
                                          ┌───────▼───────┐               │
                                          │ capture script │              │
                                          │ v4l2-ctl       │              │
                                          │ --stream-mmap   │              │
                                          │ --stream-to=   │              │
                                          │ output/*.bin   │              │
                                          └───────┬───────┘               │
                                                  │                       │
                                          ┌───────▼───────┐               │
                                          │ adc_rx.cpp     │               │
                                          │ (实时 V4L2 模式)│              │
                                          │ 或             │               │
                                          │ adc_rx.py      │               │
                                          │ (离线 .bin 回放)│              │
                                          └───────────────┘               │
```

#### 1.4.2 各层软件组件

| 层 | 组件 | 文件 | 功能 |
|----|------|------|------|
| **用户态: 雷达配置** | `carkit88c0_gmsl_example` | `src/demo_app/.../example/main.c` | SPI-over-GMSL 配置 CTRX 寄存器: TX功率、RX增益、RF频率、Sequencer 加载、启动连续 chirp 发射 |
| **用户态: Sequencer** | `sequencer_data.c` | `.../example/sequencer_data.c` | 43-word 时序程序: 25MSps ADC, 512 ramps/chirp, 定义 chirp 时序 |
| **内核态: GMSL 驱动** | `max929x.ko` | `src/modules/max929x/max929x.c` | I2C 驱动, probe 时配置 MAX9296+MAX96717: MIPI PHY、lane 映射、Double Mode(12→24bit打包)、GPIO隧道、SPI隧道、CSI输出 |
| **内核态: 雷达 V4L2** | `radar.ko` | `src/modules/radar/radar.c` | Tegra Camera Framework sub-device, 注册 V4L2 `/dev/videoN`, 定义 20 种采集模式 |
| **内核态: VI 框架** | `tegra-video` | NVIDIA 闭源 | MIPI CSI-2 硬件采集, DMA→内核buffer, V4L2 mmap streaming |
| **用户态: 数据采集** | `v4l2-ctl` | v4l-utils 包 | `VIDIOC_S_FMT` 设置格式 → `REQBUFS`+`mmap` → `STREAMON` → `DQBUF`(阻塞) → `write()` 到 `.bin` |
| **用户态: ROS2 实时** | `adc_rx.cpp` | `src/ft_rx_cpp/src/adc_rx.cpp` | 实时 V4L2 mmap 采集, 阻塞 `DQBUF`, 逐帧发布 `/adc/raw_data` |
| **用户态: ROS2 离线** | `adc_rx.py` / `adc_rx_analog.cpp` | `src/ft_framework/` / `src/ft_rx_cpp/` | 从 `.bin` 文件预加载帧, ctrx0+ctrx1 双文件逐帧拼接为完整 32 MiB |

#### 1.4.3 V4L2 帧格式与 .bin 文件布局

```
V4L2 帧参数 (单个 CTRX, 如 /dev/video0):
  width=8192, height=1024, pixelformat=RG12 (V4L2_PIX_FMT_SRGGB12)

每行 (8192 pixels = 16384 bytes):
  ┌──────────┬──────────┬──────────┬──────────┬──────────┬─────┐
  │ RX0_sam0 │ RX1_sam0 │ RX2_sam0 │ RX3_sam0 │ RX0_sam1 │ ... │  ← 4 RX 逐采样点交错
  │ (2bytes) │ (2bytes) │ (2bytes) │ (2bytes) │ (2bytes) │     │     RG12: 12-bit 数据 + 4-bit 零填充
  └──────────┴──────────┴──────────┴──────────┴──────────┴─────┘
  共 2048 个采样点 × 4 RX = 8192 pixels

1024 行:
  Row 0..511:   Group 0, 512 chirps (每行=1 chirp × 4 RX × 2048 samples)
  Row 512..1023: Group 1, 512 chirps
  
单帧大小 (单个 CTRX):  8192 × 1024 × 2 = 16,777,216 bytes = 16 MiB
单帧大小 (双 CTRX 合并): 2 × 16 MiB = 32 MiB

.bin 文件内容:
  [Frame0][Frame1][Frame2]...[FrameN]  ← N 帧连续拼接, 无帧头/分隔符

帧计数方式:
  n_frames = file_size / (8192 × 1024 × 2) = file_size / 16 MiB
```

#### 1.4.4 数据消费者的两种模式

| 模式 | 节点 | 数据路径 | 用途 |
|------|------|---------|------|
| **离线回放** | `adc_rx.py` / `adc_rx_analog.cpp` | `scripts/output/ctrx0_raw.bin` + `ctrx1_raw.bin` → 预加载到内存 → 逐帧拼接发布 | 算法开发、离线调试、无需雷达硬件 |
| **实时采集** | `adc_rx.cpp` | `/dev/radar_ctrx0` → V4L2 mmap `DQBUF`(阻塞) → 逐帧发布 | 实时感知、实际部署 |

> **注意**: `RSPS_main.py` 是离线信号处理调试工具，不属于 ROS2 数据流管线。ROS2 框架中 `.bin` 文件仅由 `adc_rx` 节点读取并发布为 ROS2 消息。

#### 1.4.5 初始化命令序列

```bash
# Step 1: Pinmux + SPI + max929x.ko + radar.ko
cd ~/Desktop/Orin-ROS/src/integration-carkit88c0-gmsl
sudo python init_jetson.py

# Step 2: (可选) CTRX 雷达参数配置 + 启动连续发射
cd ~/Desktop/Orin-ROS/src/integration-carkit88c0-gmsl/src/demo_app/boards/carkit_88c0/example/bin
sudo ./carkit88c0_gmsl_example

# Step 3: 采集 .bin 文件 (离线回放用)
cd ~/Desktop/Orin-ROS/src/integration-carkit88c0-gmsl/scripts
bash capture_video0_2048x1024.sh
# → 生成 output/ctrx0_raw.bin (20帧 × 16 MiB) + output/ctrx1_raw.bin (20帧 × 16 MiB)

# Step 4: 启动 ROS2 管线 (实时 V4L2 模式)
ros2 launch ft_radar ft_radar_launch.py
# adc_rx.cpp → /dev/radar_ctrx0 → /adc/raw_data (V4L2 实时采集)
# 或 adc_source:=analog → adc_rx_analog.cpp → .bin 文件回放
```

### 1.5 全局时间戳要求

> **所有 Rx 节点必须在数据采集第一时间注入全局统一时间戳，精度为微秒（μs）。**

时间戳贯穿整个数据链路，从采集→处理→可视化→日志，全程透传不丢失。所有输出文件以时间戳（μs）命名。

---

## 二、系统架构与数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          第一层：数据采集层 (Rx Nodes)                        │
│                                                                             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐               │
│  │  ADC Rx      │    │  Camera Rx   │    │  Vehicle Data Rx │               │
│  │  轮询模式     │    │  轮询模式     │    │  混合模式         │               │
│  │  硬件实际频率 │    │  硬件实际频率 │    │  CAN读取线程      │               │
│  │  32MiB/frame │    │  ~16KB/frame │    │  + 定时发布50Hz   │               │
│  │  1024×8×2048 │    │  (TBD)       │    │  (TBD)           │               │
│  └──────┬───────┘    └──────┬───────┘    └────────┬─────────┘               │
│         │ pub               │ pub                  │ pub                     │
└────────────────────────────┼──────────────────────────────────────────────┘
          │                   │                      │
          ▼                   ▼                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       第二层：雷达信号处理层 (RSP Nodes)                      │
│                                                                             │
│  ┌──────────────────┐         ┌──────────────────┐                          │
│  │  R SP MIL Python │         │  R SP Cuda       │                          │
│  │  (rsp_py)        │         │  (rsp_cu)        │                          │
│  │  solving velocity│         │  solving velocity│                          │
│  │  ambiguity       │         │  ambiguity       │                          │
│  └────────┬─────────┘         └────────┬─────────┘                          │
│           │ pub                        │ pub                                │
└───────────┼────────────────────────────┼────────────────────────────────────┘
            │                            │
            ▼                            ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        第三层：高级感知与可视化层                              │
│                                                                             │
│  ┌──────────────────┐    ┌──────────────┐    ┌──────────────┐               │
│  │ 3D Object Detect │    │ Rviz_radar   │    │ Rviz_image   │               │
│  │ AI Model         │    │ det_list     │    │ image        │               │
│  │ det_list→obj_list│    │ obj_list     │    │              │               │
│  └────────┬─────────┘    └──────────────┘    └──────────────┘               │
│           │ pub                                                             │
└───────────┼─────────────────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          第四层：日志记录层 (Logging)                         │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  7个独立开关控制任意通道录制                                           │   │
│  │  ADC max 100帧 / 其他 max 1000帧                                      │   │
│  │  输入: ADC + image + Det_List + RnNciData + Ego + ObjList            │   │
│  │  输出: adc.bin, image.jpg, det_list.pcd+csv, rdCell.csv,             │   │
│  │        rxNci.bin, ego_motion.csv, obj_list.csv                       │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、各节点详细开发需求

### 3.1 Node: ADC Rx（雷达ADC数据接收节点）

#### 基本规格

| 参数 | 值 |
|------|-----|
| 帧率 | **硬件实际频率** (轮询模式, 阻塞 V4L2 DQBUF) |
| 每组数据量 | **32 MiB** (16T16R ctrx0+ctrx1 双文件合并) |
| 数据结构 | 每 chirp **2048 个 int16 采样点**，共 **1024 chirps × 8 RX**|
| 时间戳 | ROS 系统时间 (`get_clock().now()`), 微秒精度 |

#### 数据维度解读

```
ADC 数据形状: [1024 chirps × 8 RX × 2048 samples] × int16(2 bytes)
           = (ctrx0: 512 chirps×2 groups×4 RX) + (ctrx1: 512 chirps×2 groups×4 RX)
           = 33,554,432 bytes ≈ 32 MiB
```

#### 开发任务

- [ ] **消息定义**：定义 ADC 原始数据消息类型，包含：
  ```
  std_msgs/Header header          # 含全局时间戳(μs)
  uint32 num_chirps               # 1024 (512 chirps × 2 groups)
  uint32 num_rx_antennas          # 8 (ctrx0:4 + ctrx1:4)
  uint32 num_samples_per_chirp    # 2048
  int16[] data                    # 1024×8×2048 = 16,777,216 个采样点 (ctrx0+ctrx1合并)
  ```
- [ ] **零拷贝通信**：32 MiB/帧 × 15 Hz = **480 MiB/s** 吞吐，使用 ROS2 Intra-process 或共享内存（ctrx0+ctrx1 双文件合并）
- [x] **采集频率控制**：改为轮询模式 (阻塞 V4L2 DQBUF), 频率由硬件实际发送速率决定
- [x] **时间戳注入**：在数据采集第一时间打 ROS 系统时间戳 (`get_clock().now()`)
- [ ] **缓冲区管理**：设计环形缓冲区，建议大小 ≥ 3 帧（96MB），防止处理延迟导致丢帧
- [ ] **参数化配置**：帧率、数据维度等通过 YAML 参数文件配置，支持运行时调整

---

### 3.2 Node: Camera Rx（相机数据接收节点）

#### 基本规格

| 参数 | 值 |
|------|-----|
| 硬件 | **Rmoncam A2 1080P** USB UVC 摄像头 |
| 接口 | USB 3.0, 驱动 `uvcvideo` |
| 设备路径 | `/dev/camera_capture` (udev 持久化符号链接 → `/dev/video2`) |
| 帧率 | **硬件实际频率** (轮询模式, 阻塞 `camera.read()`) |
| 分辨率 | **1920×1080** |
| 像素格式 | **MJPEG** (压缩) → OpenCV 自动解码为 **BGR8** |
| 每帧数据量 | **~100-300 KB** (MJPEG 压缩, 取决于画面复杂度) → 解压后 **~6 MB** |
| 时间戳 | ROS 系统时间 (`get_clock().now()`), 微秒精度 |
| 实现方式 | C++ (OpenCV `cv::VideoCapture` + `cv_bridge`) + Python (同接口) |
| 采集方式 | **轮询线程**: 阻塞读取 → 即时打戳 → 发布 (硬件频率驱动, 非定时器) |

#### 支持的像素格式

| 格式 | 最大分辨率@帧率 | 说明 |
|------|:--------------:|------|
| **MJPG** | 1920×1080 @ 30fps | 推荐, Motion-JPEG 压缩, OpenCV 自动解码 |
| YUYV | 1920×1080 @ 5fps | 未压缩 YUV 4:2:2, 低帧率 |

#### 开发任务

- [x] **V4L2 采集实现**：使用 OpenCV `cv::VideoCapture` + `cv::CAP_V4L2` 后端，通过 udev 符号链接 `/dev/camera_capture` 打开设备
- [x] **格式配置**：MJPEG 像素格式 + 1920×1080 + 30fps，OpenCV 自动解码为 BGR8
- [x] **断线自动重连**：每 90 帧 (≈3s) 尝试重连，重连期间发布空图像维持拓扑
- [x] **时间戳注入**：与 ADC Rx 使用同一时钟源，确保多传感器时间同步
- [x] **双语言实现**：C++ (`camera_rx.cpp`) + Python (`camera_rx.py`)，YAML 配置统一管理
- [x] **udev 持久化命名**：USB 摄像头 → `/dev/camera_capture`（规则见 `scripts/99-ft-sensors.rules`）

---

### 3.3 Node: Vehicle Data Rx（车辆数据接收节点）

#### 基本规格

| 参数 | 值 |
|------|-----|
| 发布频率 | **50 Hz** (可配置, 定时器控制) |
| CAN 读取 | **独立轮询线程**, 持续读取 CAN 总线更新 buffer |
| 架构模式 | **混合模式**: CAN 读取线程 → buffer (mutex) → 定时器发布 |
| 数据内容 | 自车运动数据（ego motion） |
| 默认值机制 | 数据丢失时使用预设默认值 |
| 超时间隔 | **20ms** (1 个周期 @ 50Hz), 超过后切换为默认值 |

#### 架构说明

```
CAN Bus ──→ [CAN read thread: 持续 poll/read] ──→ buffer (mutex) ──→ [timer callback: 按 fps 发布]
```

- **CAN 读取线程**: 持续轮询 CAN 总线, 将最新数据更新到线程安全的 buffer。CAN 数据到来即更新, 不受定时器限制。
- **定时器回调**: 按 `fps` 频率 (默认 50Hz) 从 buffer 取最新数据快照, 打上 ROS 系统时间戳后发布。
- **超时检测**: 超过 `timeout_cycles` 个周期 (默认 1 周期 = 20ms) 未收到 CAN 数据, 自动切换为默认安全值。
- **未接入 CAN 时**: 定时器正常发布默认值 (`is_default=True`), CAN 线程休眠等待。

#### 开发任务

- [x] **默认值机制实现**: 已实现
- [x] **超时检测**: 已实现 (20ms 超时间隔)
- [x] **混合架构**: CAN 读取线程 + buffer + 定时器发布, `fps` 参数控制发布频率
- [x] **时间戳注入**: 使用 ROS 系统时间 (`get_clock().now()`)
- [ ] **CAN/ETH 总线接入**: 替换 `_can_read_loop()` 中的 TODO 占位为实际 CAN 读取逻辑

---

### 3.4 Node: R SP MIL Python & R SP Cuda（雷达信号处理节点）

#### 启动模式需求（重要变更）

> **可以启动二者其中任意一个，或同时启动二者。需要便于使用各种启动模式（快捷命令行或便捷参数修改），各种启动模式下都不影响整体框架运行。**

#### 四种启动模式

| 模式 | 命令/参数 | 说明 |
|------|----------|------|
| **仅 Python** | `ros2 launch ft_radar rsp_mode:=python` | 开发调试，算法验证 |
| **仅 CUDA** | `ros2 launch ft_radar rsp_mode:=cuda` | 生产部署，GPU加速 |
| **双路并行** | `ros2 launch ft_radar rsp_mode:=both` | 对比验证，结果一致性检查 |
| **双路对比** | `ros2 launch ft_radar rsp_mode:=both_compare` | 并行运行 + 自动对比输出差异 |

#### 开发任务

- [ ] **Launch 文件设计**：
  ```python
  # ft_radar_launch.py
  rsp_mode = LaunchConfiguration('rsp_mode', default='cuda')
  
  # 条件启动逻辑
  python_enabled = PythonExpression(["'", rsp_mode, "' in ['python', 'both', 'both_compare'"])
  cuda_enabled = PythonExpression(["'", rsp_mode, "' in ['cuda', 'both', 'both_compare'"])
  ```
- [ ] **话题命名隔离**：双路运行时输出不同话题，避免冲突：
  ```
  /processing/radar/det_list_python    # Python路径输出
  /processing/radar/det_list_cuda      # CUDA路径输出
  /processing/radar/det_list           # 主输出（根据模式选择）
  ```
- [ ] **下游节点适配**：Rviz_radar、Logging、3D Object Detection 节点需能根据配置选择订阅哪个 det_list 话题，或同时订阅两个
- [ ] **速度模糊消除算法**：
  - 输入：ADC 数据 + vdd（车辆速度用于补偿）
  - 核心：FFT 多普勒处理 + 速度模糊消除（利用 `ambgt` 速度模糊窗宽信息）
  - 输出：Det List（雷达目标列表）
- [ ] **CUDA 版本优化**：使用 cuFFT、CUDA Graphs、统一内存等 Jetson 优化技术
- [ ] **一致性验证工具**（both_compare 模式）：自动对比两路输出的 Det List 差异，生成差异报告

---

### 3.5 Node: 3D Object Detection（3D目标检测节点）

#### 开发任务

- [ ] **AI 模型部署**：TensorRT 推理引擎，支持模型热更新
- [ ] **输入**：det_list（雷达检测目标列表）
- [ ] **输出**：obj_list，字段与 `FT_radar_dataset_requirement.md` 中雷达目标 CSV 一致：

  | 字段 | 类型 | 单位 | 描述 |
  |------|------|------|------|
  | `object_id` | uint64 | - | 目标跟踪ID |
  | `tracked_times` | uint64 | - | 跟踪帧数 |
  | `score` | float32 | [0,1] | 置信度/障碍物概率 |
  | `x, y, z` | float32 | m | 车辆坐标系下bbox中心点 |
  | `l, w, h` | float32 | m | bbox长宽高 |
  | `yaw` | float32 | rad | 航向角 |
  | `vx_absolute, vy_absolute, vz_absolute` | float32 | m/s | 对地速度（车辆坐标系） |
  | `moving_state` | uint8 | enum | 0:moving, 1:stationary, 2:oncoming, 3:cross, 4:stopped, 255:unknown |

- [ ] **坐标系转换**：确保输出在车辆坐标系（前左上，原点后轴中心接地点）

---

### 3.6 Node: Rviz_radar（雷达数据RViz可视化节点）

> **确认：本节点没有新增输入。**

#### 输入

| 来源 | 数据 |
|------|------|
| R SP MIL Python / R SP Cuda | `det_list`（雷达检测目标列表） |
| 3D Object Detection | `obj_list`（3D目标识别结果） |
| Rviz_Ruler | `ruler`（标尺/参考数据） |

#### 开发任务

- [ ] **RViz2 显示**：同时渲染 det_list（点云/检测点）和 obj_list（3D bbox）
- [ ] **坐标系管理**：使用 tf2 管理车辆坐标系、雷达坐标系、Local 坐标系
- [ ] **多 det_list 源适配**：当 RSP 双路运行时，可选择显示哪一路或同时显示两路（不同颜色）

---

### 3.7 Node: Rviz_image（图像RViz可视化节点）

#### 输入

| 来源 | 数据 |
|------|------|
| Camera Rx | `image`（相机图像帧） |

#### 开发任务

- [ ] 在 RViz2 中显示相机图像（使用 `rviz_default_plugins/Image` 或自定义插件）
- [ ] 30Hz 刷新率适配

---

### 3.8 Node: Rviz_Ruler（RViz标尺节点）

#### 开发任务

- [ ] 提供 RViz 可视化所需的标尺/参考基准数据（距离标尺、ROI区域、坐标参考等）
- [ ] 具体 ruler 内容需与架构师确认，预留参数化配置接口

---

### 3.9 Node: Logging（数据日志记录节点）— 重大变更

#### 输入

| 来源 | 数据 | 备注 |
|------|------|------|
| 配置文件 | `calibration.yaml` | 标定文件，启动时加载 |
| ADC Rx | `ADC` | 雷达原始ADC数据 |
| Camera Rx | `image` | 相机图像帧 |
| R SP 节点 | `Det_List` | 雷达检测目标列表 |
| Vehicle Data Rx | `vdd` | 车辆动态数据（→ ego_motion） |
| 3D Object Detection | `obj_list` | 3D目标列表 |

#### 5个独立开关控制

> **Logging开关作用是控制任意通道的数据进行录制。**

```yaml
logging:
  switches:
    enable_adc: true          # 开关1: ADC原始数据
    enable_image: true        # 开关2: 相机图像
    enable_det_list: true     # 开关3: 雷达检测列表
    enable_ego_motion: true   # 开关4: 自车运动数据
    enable_obj_list: true     # 开关5: 3D目标列表
```

- [ ] 每个开关独立控制对应通道的录制启停
- [ ] 支持运行时动态切换（`ros2 param set`）
- [ ] 开关状态记录到日志元数据中

#### 帧数限制

| 数据类型 | 最大帧数 | 存储估算 |
|----------|---------|---------|
| ADC | **100 帧** | 100 × 32 MiB = **3.2 GB** |
| image | **1000 帧** | 1000 × 16KB ≈ **16 MB**（若为JPEG则更大） |
| Det_List | **1000 帧** | 取决于每帧目标数 |
| ego_motion | **1000 帧** | 1000 × ~100B ≈ **100 KB** |
| obj_list | **1000 帧** | 取决于每帧目标数 |

- [ ] 达到上限后的策略需确认（停止记录 vs 循环覆盖），建议默认**停止记录并通知**
- [ ] 实时显示各通道已录制帧数/上限

#### 输出文件（与 FT_radar_dataset_requirement.md 对齐）

| 输出文件 | 格式 | 说明 | 命名规则 |
|----------|------|------|----------|
| `adc.bin` | 二进制 | ADC原始数据 | 连续存储，含帧头时间戳 |
| `image.jpg` | JPEG | 相机图像 | `{timestamp_us}.jpg` |
| `det_list.pcd` | PCD | 雷达检测目标 (19 字段, spec §5.3) | `{timestamp_us}.pcd` |
| `det_list.csv` | CSV | 雷达检测目标 (22 列, spec §6.3) | `{timestamp_us}.csv` |
| `rdCell_list.csv` | CSV | RD Cell List (13 字段展开, spec §7.3) | `{timestamp_us}.csv` |
| `rxNci.bin` | BIN | Rx NCI 原始 float32 (spec §8.3) | `{timestamp_us}.bin` |
| `ego_motion.csv` | CSV | 自车运动数据 | 单文件，含全部时间帧 |
| `obj_list.csv` | CSV | 3D目标列表 | `{timestamp_us}.csv` |

> 数据格式全面对齐 `FT_FVR60_XD_radar_dataset_requirement.md` §3~§9。

#### det_list.pcd 格式

- [ ] 标准 PCL PCD 格式 (v0.7)，支持 ASCII 和 Binary 两种 DATA 模式。

pycd4 是一个用于处理 PCD（Point Cloud Daa）文件的现代 Python 库，使用 Python 3 编写，其核心是 PointCloud 类，所有 PCD 文件都会读取为这个对象。

PCD是点云库（PCL，Point Cloud Library）的原生文件格式，而v0.7是官方指定的标准版本。

#### calibration.yaml 处理

- [ ] 启动时加载标定文件
- [ ] 将标定文件内容嵌入日志元数据，或单独保存副本
- [ ] 标定文件内容格式遵循 `FT_radar_dataset_requirement.md` 第4节定义

#### 开发任务清单

- [ ] **异步写入架构**：独立写入线程 + 无锁队列，不阻塞主处理流程
- [ ] **时间戳对齐**：以雷达帧时钟为基准，其他数据按时间戳最近邻匹配
- [ ] **磁盘空间监控**：实时监测剩余空间，低于阈值告警
- [ ] **文件命名**：所有帧级文件使用 `{timestamp_us}.xxx` 命名
- [ ] **JPEG硬件编码**：使用 Jetson NVENC 加速 image.jpg 编码
- [ ] **ego_motion.csv 单文件写入**：与帧级文件不同，ego_motion 是单文件追加写入

---

## 四、数据格式与消息定义（融合 FT_radar_dataset_requirement.md）

### 4.1 坐标系定义

```
        X (前方)
        ↑
        |
        |
Y ←─────● (后轴中心接地点)
        |
        |
        Z: 垂直向上 (右手坐标系)
```

| 轴 | 方向 | 正值含义 |
|----|------|---------|
| X | 车辆正前方 | 前方 |
| Y | 车辆左侧 | 左侧 |
| Z | 垂直向上 | 上方 |
| 原点 | 后轴中心接地点 | - |

**Local 坐标系**：车辆上电时的后轴中心位置，轴方向与上电时刻车辆坐标系一致。

### 4.2 雷达点云数据字段（det_list → PCD/CSV, 对齐 spec §5.3/§6.3）

Logging 输出的 `det_list.pcd` 和 `det_list.csv` 字段定义 (19 字段 + 3 帧级)：

| # | 字段名 | 类型 | 单位 | 描述 |
|---|--------|------|------|------|
| 1 | `u32TimeStamp` | uint32 | μs | 帧时间戳 |
| 2 | `u16FrameID` | uint16 | - | 帧序号 |
| 3 | `u16DetObjNum` | uint16 | - | 检测目标数 |
| 4 | `f32XPos` | float32 | m | 车辆系 X（前方） |
| 5 | `f32YPos` | float32 | m | 车辆系 Y（左侧） |
| 6 | `f32ZPos` | float32 | m | 车辆系 Z（上方） |
| 7 | `f32Range` | float32 | m | 径向距离 |
| 8 | `f32Speed` | float32 | m/s | 目标合速度 |
| 9 | `f32AzimuthAng` | float32 | rad | 方位角 |
| 10 | `f32EleAng` | float32 | rad | 俯仰角 |
| 11 | `f32SNRdB` | float32 | dB | 信噪比 |
| 12 | `f32RcsdB` | float32 | dBsm | RCS |
| 13 | `f32PowerdB` | float32 | dB | 回波功率 |
| 14 | `u32ObjSameRV` | uint32 | - | 同距离同速度标志 |
| 15 | `u16RdCellIdx` | uint16 | - | RD 单元总索引 |
| 16 | `u16RangeIdx` | uint16 | - | 距离索引 |
| 17 | `u16DopplerIdx` | uint16 | - | 多普勒索引 |
| 18 | `u8AzimuthIdx` | uint8 | - | 方位角索引 |
| 19 | `u8ElevationIdx` | uint8 | - | 俯仰角索引 |
| 20 | `u16PeakVal` | uint16 | - | 峰值强度 |
| 21 | `u16SinAzimSNRLin` | uint16 | - | 方位 sin 域 SNR |
| 22 | `u16SinElevSNRLin` | uint16 | - | 俯仰 sin 域 SNR |

**关键过滤规则**：

| 过滤器 | 条件 | 默认阈值 |
|--------|------|----------|
| ROI | x/y/z 超出包围盒 | x∈[-80,200], y∈[-40,40], z∈[-15,15] m |
| 高度 | \|z\| >= \|x\| × 0.1 + 2 | 自适应 |
| RCS | 10m内: rcs_db≤-40; 10m外: rcs_db≤-20 | dBsm |
| 存在概率 | det_conf < 30 | - |
| SNA | idx == 255 | - |
| 模糊概率 | det_ambig_state >= 40 | - |

### 4.2b RD Cell List 数据字段 (对齐 spec §7.3)

| # | 字段名 | 类型 | 说明 |
|---|--------|------|------|
| 1 | `u32FrameTimeStamp` | uint32 | 帧时间戳 (μs) |
| 2 | `u16FrameId` | uint16 | 帧 ID |
| 3 | `u16NofRdCell` | uint16 | RD Cell 数量 |
| 4 | `u8Index_Idletime` | uint8 | 空闲时间索引 |
| 5 | `u16Rb` | uint16 | 距离 bin |
| 6 | `u16Db` | uint16 | 多普勒 bin |
| 7 | `f32PowRbNci_Q7dB-[0..2]` | float32×3 | 距离向 NCI 功率 (展开为 -0/-1/-2) |
| 8 | `f32PowDbNci_Q7dB-[0..2]` | float32×3 | 多普勒向 NCI 功率 (展开为 -0/-1/-2) |
| 9 | `f32PeakPowVchNci_Q7dB` | float32 | 峰值功率 |
| 10 | `f32NoiseNci_Q7dB` | float32 | 噪声功率 |
| 11 | `u8RdValidFlag` | uint8 | 有效性标志 |
| 12 | `u8RdPeakFlag` | uint8 | 峰值标志 |
| 13 | `sVch-[0..255]_r / sVch-[0..255]_im` | int32×512 | 通道复数数据 (256 复数 → 256r + 256im) |

### 4.2c Rx NCI BIN 数据 (对齐 spec §8.3)

每帧 RD Map 二维数组: `float32[rx_nci_rows × rx_nci_cols]` (典型 512 × 1025), 原始字节序 LE。

### 4.3 3D目标数据字段（obj_list）

| # | 字段名 | 类型 | 单位 | 描述 |
|---|--------|------|------|------|
| 1 | `object_id` | uint64 | - | 目标跟踪ID |
| 2 | `tracked_times` | uint64 | - | 跟踪帧数 |
| 3 | `score` | float32 | [0,1] | 置信度/障碍物概率 |
| 4 | `x` | float32 | m | 车辆系bbox中心X |
| 5 | `y` | float32 | m | 车辆系bbox中心Y |
| 6 | `z` | float32 | m | 车辆系bbox中心Z |
| 7 | `l` | float32 | m | bbox长度 |
| 8 | `w` | float32 | m | bbox宽度 |
| 9 | `h` | float32 | m | bbox高度 |
| 10 | `yaw` | float32 | rad | 航向角 |
| 11 | `vx_absolute` | float32 | m/s | 对地速度X |
| 12 | `vy_absolute` | float32 | m/s | 对地速度Y |
| 13 | `vz_absolute` | float32 | m/s | 对地速度Z |
| 14 | `moving_state` | uint8 | enum | 0:moving, 1:stationary, 2:oncoming, 3:cross, 4:stopped, 255:unknown |

### 4.4 自车运动数据字段（ego_motion）

| # | 字段名 | 类型 | 单位 | 描述 |
|---|--------|------|------|------|
| 1 | `timestamp_us` | uint64 | μs | 时间戳 |
| 2 | `vx` | double | m/s | 车速 |
| 3 | `yaw_rate` | double | rad/s | 偏航角速度 |
| 4 | `steering_angle` | double | rad | 转向角度 |
| 5 | `ax` | double | m/s² | 纵向加速度 |
| 6 | `ay` | double | m/s² | 横向加速度 |
| 7 | `gear` | uint8 | - | 挡位 |

**频率要求**：≥50 Hz（间隔 ≤20ms），最大允许间隔 500ms。

### 4.5 标定文件格式（calibration.yaml）

```yaml
%YAML:1.0
---
sensor_name: "radar_front_center_ft"
sensor_type: ft_fvr60_eo
vehicle_xyz: front_left_up
r_s2b: [3.141563, 0.01370774, -1.034906e-09]   # Rodrigues旋转向量 (rad), 传感器→车体
t_s2b: [4.052, -0.17, 0.677]                     # 平移向量 (m), 传感器→车体
```

**雷达位置与目录名映射**：

| 目录名包含 | 雷达位置 | pos值 |
|-----------|---------|-------|
| `front_center` | 前方中央 | 0 |
| `front_left` | 左前角 | 1 |
| `side_left` | 左侧 | 2 |
| `rear_left` | 左后角 | 3 |
| `rear_center` | 后方中央 | 4 |
| `rear_right` | 右后角 | 5 |
| `side_right` | 右侧 | 6 |
| `front_right` | 右前角 | 7 |

---

## 五、时间同步机制

### 5.1 全局时间戳方案

| 项目 | 规格 |
|------|------|
| 时钟源 | ROS 系统时间 `get_clock().now()` (开发阶段) / PTP硬件时钟（生产阶段） |
| 精度 | 微秒（μs） |
| 注入位置 | 所有 Rx 节点数据采集第一时间 (轮询线程/定时器回调) |
| 透传规则 | 后续节点不得覆盖，仅在消息头 `header.stamp` 中传递 |

### 5.2 多传感器时间对齐策略

- **ADC Rx**：轮询模式, 硬件实际频率 (V4L2 阻塞 DQBUF), 每帧即时 ROS 时间戳
- **Camera Rx**：轮询模式, 硬件实际频率 (阻塞 `camera.read()`), 每帧即时 ROS 时间戳
- **Vehicle Data Rx**：混合模式, CAN 读取线程持续更新 buffer, 定时器按 50Hz 发布时打 ROS 时间戳
- **Logging 对齐**：Camera/Vehicle 数据按时间戳最近邻匹配到雷达帧
- **ego_motion.csv**：单文件按时间戳排序存储，程序处理时按时间戳交替处理

---

## 六、启动模式与配置管理

### 6.1 Launch 文件设计

```bash
# 模式1: 仅Python RSP
ros2 launch ft_radar ft_radar_launch.py rsp_mode:=python

# 模式2: 仅CUDA RSP（默认生产模式）
ros2 launch ft_radar ft_radar_launch.py rsp_mode:=cuda

# 模式3: 双路并行
ros2 launch ft_radar ft_radar_launch.py rsp_mode:=both

# 模式4: 双路对比
ros2 launch ft_radar ft_radar_launch.py rsp_mode:=both_compare

# 自定义Logging开关
ros2 launch ft_radar ft_radar_launch.py \
  rsp_mode:=cuda \
  log_adc:=true \
  log_image:=false \
  log_det_list:=true \
  log_ego_motion:=true \
  log_obj_list:=true
```

### 6.2 参数配置文件结构

```yaml
# ft_radar_params.yaml
system:
  global_timestamp_source: "monotonic"  # 或 "ptp"
  timestamp_unit: "us"

adc_rx:
  fps: 15
  num_chirps: 512
  num_rx_antennas: 16
  num_samples: 2048
  dtype: "int16"
  frame_size_mb: 32

camera_rx:
  fps: 30
  image_width: 1920                         # 图像宽度 (px)
  image_height: 1080                        # 图像高度 (px)
  device_path: "/dev/camera_capture"        # udev 持久化符号链接
  pixel_format: "MJPG"                      # MJPEG@30fps | YUYV@5fps
  reconnect_interval_frames: 90             # 断线重试间隔
  line: 0                                   # 预留接口，待确认
  nof_line: 0                               # 预留接口，待确认

vehicle_data_rx:
  fps: 50
  timeout_cycles: 3
  defaults:
    vx: 0.0
    yaw_rate: 0.0
    steering_angle: 0.0
    ax: 0.0
    ay: 0.0
    gear: 1

rsp:
  mode: "cuda"              # python | cuda | both | both_compare
  velocity_ambiguity_solver: "multi_prf"

logging:
  output_dir: "/data/ft_radar_dataset"
  max_frames:
    adc: 100
    image: 1000
    det_list: 1000
    ego_motion: 1000
    obj_list: 1000
  switches:
    enable_adc: true
    enable_image: true
    enable_det_list: true
    enable_ego_motion: true
    enable_obj_list: true
  calibration_file: "calibration/radar_front_center_ft.yaml"
```

---

## 七、ROS2 消息类型定义

### 7.1 自定义消息包 `ft_radar_msgs`

```
# AdcRawData.msg
std_msgs/Header header
uint32 num_chirps
uint32 num_rx_antennas
uint32 num_samples_per_chirp
int16[] data                    # [num_chirps × num_rx_antennas × num_samples_per_chirp]

# DetList.msg
std_msgs/Header header
DetPoint[] points

# DetPoint.msg
float32 x                       # 车辆系
float32 y                       # 车辆系
float32 z                       # 车辆系
float32 range                   # 雷达系
float32 azimuth                 # 雷达系
float32 elevation               # 雷达系
float32 rcs                     # dBsm
float32 snr                     # dB
float32 ambgt                   # m/s
uint8 exist_prob                # [0,255]
uint8 multi_tgt_prob            # [0,255]
uint8 ambgt_prob                # [0,255]
float32 raw_doppler             # m/s
uint8 idx                       # 多普勒索引

# ObjList.msg
std_msgs/Header header
Object3D[] objects

# Object3D.msg
uint64 object_id
uint64 tracked_times
float32 score
float32 x
float32 y
float32 z
float32 l
float32 w
float32 h
float32 yaw
float32 vx_absolute
float32 vy_absolute
float32 vz_absolute
uint8 moving_state              # 0:moving,1:stationary,2:oncoming,3:cross,4:stopped,255:unknown

# EgoMotion.msg
std_msgs/Header header          # timestamp_us in header.stamp
float64 vx                      # m/s
float64 yaw_rate                # rad/s
float64 steering_angle          # rad
float64 ax                      # m/s²
float64 ay                      # m/s²
uint8 gear
bool is_default                 # 是否为默认值
```

---

## 八、开发路线图

### Phase 1: 基础设施（2-3周）

- [ ] 搭建 ROS2 Humble + Jetpack 5.1.2 开发环境
- [ ] 创建 `ft_radar_msgs` 消息包，定义所有消息类型
- [ ] 创建 `ft_radar_launch` 启动包，实现4种RSP启动模式
- [ ] 实现参数配置系统（YAML + ROS2参数）
- [ ] 实现全局时间戳模块（微秒精度）

### Phase 2: 数据采集层（2-3周）

- [x] 实现 ADC Rx 节点（15Hz, 32 MiB/帧, 零拷贝, ctrx0+ctrx1双文件合并）✅ **已完成**
- [x] 实现 Camera Rx 节点（30Hz, Rmoncam A2 1080P, MJPEG→BGR8, V4L2 真实采集）✅ **已完成**
- [x] 实现 Vehicle Data Rx 节点（50Hz, 默认值机制, is_default=True）✅ **占位完成**
- [x] udev 持久化设备命名（camera_capture / radar_ctrx0 / radar_ctrx1）✅ **已完成**
- [x] 时间戳注入与透传验证 ✅ **已完成**

### Phase 3: 雷达信号处理（4-6周）

- [ ] **Python 版本 RSP 算法实现** ⬅ 当前待开发
  - [ ] ADC int16 数据 reshape: `(num_rows, num_chirps_per_row, num_samples_per_chirp)` → `(1024, 8, 2048)`
  - [ ] Range-FFT: 沿 sample 维度 (2048点) 加窗 + FFT → 距离谱
  - [ ] Doppler-FFT: 沿 chirp 维度 (1024点) 加窗 + FFT → 距离-多普勒谱
  - [ ] 2D-CFAR: 恒虚警率检测, 提取超过阈值的 range-doppler bin
  - [ ] 角度估计: 8 RX 通道间相位差 → 方位角/俯仰角 (MUSIC/FFT)
  - [ ] 速度解模糊: 利用 `ambgt` 速度模糊窗宽信息
  - [ ] 输出 DetPoint 14 字段, SNR 阈值过滤
  - [ ] 替代当前空 DetList 占位发布
- [ ] CUDA 版本 RSP 开发与优化（cuFFT, CUDA Graphs）
- [ ] 双路并行运行与结果对比工具
- [ ] Launch 文件4种模式完整实现

### Phase 4: 高级感知与可视化（3-4周）

- [ ] 3D Object Detection 节点（TensorRT部署）
- [ ] Rviz_radar 可视化插件（det_list + obj_list）
- [ ] Rviz_image 图像显示
- [ ] Rviz_Ruler 标尺节点
- [ ] TF坐标系管理（车辆系、雷达系、Local系）

### Phase 5: Logging系统（2-3周）

- [ ] 5个独立开关实现
- [ ] 帧数限制与计数器
- [ ] 输出文件生成（adc.bin, image.jpg, det_list.csv, det_list.pod, ego_motion.csv, obj_list.csv）
- [ ] calibration.yaml 加载与记录
- [ ] 异步写入与磁盘监控
- [ ] 时间戳对齐与文件命名

### Phase 6: 集成测试与优化（2-3周）

- [ ] 全链路集成测试
- [ ] 性能基准测试（端到端延迟、吞吐量）
- [ ] 零拷贝通信优化
- [ ] 错误处理与容错机制
- [ ] 文档完善

---

## 九、已确认的关键问题（2026-07-01 更新）

| # | 问题 | 结论 | 影响范围 | 状态 |
|---|------|------|---------|:------:|
| 1 | Camera Rx 的 `line` 和 `nofLine` 具体数值？ | Rmoncam A2 1080P USB UVC, MJPEG 1920×1080@30fps, ~100-300KB/帧 | Camera Rx | ✅ |
| 2 | Logging 帧数达到上限后是停止记录还是循环覆盖？ | **循环覆盖**: 达到上限后删除最旧文件, 保留最新 N 帧。ego_motion.csv 保留最新 N 行后重写文件。 | Logging | ✅ 已实现 |
| 3 | `ego_motion.csv` 与原 `vdd.csv` 是否完全同一份数据仅重命名？ | **跳过** (不适用) | — | — |
| 4 | 5个 Logging 开关是否就是 adc/image/det_list/ego_motion/obj_list？ | **是的**, 目前仅这 5 个独立开关 | Logging | ✅ 已确认 |
| 5 | Rviz_Ruler 的 `ruler` 数据具体含义？ | **距离标尺**: 沿 X/Y 轴等间隔放置数字标签 + 刻度线 + 轴线, 供 RViz 中参考实际距离 | Rviz_Ruler | ✅ 已确认 |
| 6 | 双路RSP运行时，下游节点订阅哪个det_list话题？ | **优先 CUDA, 回退 Python**: `object_detection_3d` 同时订阅两个话题, CUDA 数据优先处理; 若 CUDA 超过 1s 无数据则回退到 Python RSP | RSP, 下游节点 | ✅ 已实现 |
| 7 | ADC 32 MiB/帧的零拷贝方案选择？ | **FastDDS SHM 共享内存传输** (默认), `uint8[]` 消息类型 + 128MB SHM 段, 全链路零拷贝 | ADC Rx, RSP | ✅ 已完成 (2026.7.2) |

---

## 十、总结

本开发方案基于新版架构图和 `FT_radar_dataset_requirement.md` 数据规范，全面整合了以下核心变更：

1. **全局时间戳机制**：所有 Rx 节点注入微秒精度全局时间戳，全程透传
2. **ADC Rx 规格明确**：15Hz、32 MiB/帧、1024×8×2048 数据结构（16T16R ctrx0+ctrx1合并），需零拷贝传输
3. **Camera Rx 已完成**: Rmoncam A2 1080P USB UVC 摄像头接入，1920×1080 MJPEG@30fps, udev 持久化命名 `/dev/camera_capture`
4. **RSP 四种启动模式**：python / cuda / both / both_compare，通过 launch 参数便捷切换
5. **Logging 5独立开关**：任意通道独立控制录制，ADC 100帧/其他1000帧限制
6. **输出格式全面对齐**：与 FT_radar_dataset_requirement.md 的6类数据文件完全对应
7. **坐标系与字段定义**：车辆坐标系（前左上）、雷达点云14字段、3D目标14字段、ego_motion 7字段

**下一步行动**：优先确认 P0 级问题（Camera数据格式），同时启动 Phase 1 基础设施搭建。

---

## 十一、当前开发状态（2026-06-30）

### 数据流状态总览

```
✅ 真实数据              ⚠️ 占位/待接入           ❌ 待实现算法

[ADC Rx] ───────────→ [RSP] ───────────→ [Rviz]
 ✅ 轮询模式              ⚠️ 空DetList        ✅ 直通显示
 阻塞 V4L2 DQBUF       等待算法实现          无检测点渲染
 硬件实际频率 + ROS时间戳

[Camera Rx] ─────────→ [Rviz_Image]
 ✅ 轮询模式             ✅ 直通显示
 阻塞 camera.read()
 硬件实际频率 + ROS时间戳

[Vehicle Rx] ─────────→ [RSP/EgoMotion]
 ✅ 混合模式             下游 is_default=True
 CAN读取线程+buffer      待CAN/ETH总线接入
 + 定时发布50Hz
 + 超时20ms检测
```

### 各节点实现状态

| 节点 | 状态 | 数据来源 | 待完成 |
|------|:----:|---------|--------|
| **ADC Rx** (C++/Python) | ✅ 完成 | 轮询模式, 阻塞V4L2 DQBUF, ctrx0+ctrx1双.bin合并, ROS时间戳 | — |
| **Camera Rx** (C++/Python) | ✅ 完成 | 轮询模式, 阻塞camera.read(), Rmoncam A2 1080P MJPEG→BGR8, ROS时间戳 | — |
| **Vehicle Data Rx** (C++/Python) | ✅ 完成 | 混合模式: CAN读取线程 + buffer(mutex) + 定时发布50Hz, 超时20ms检测, ROS时间戳 | 接入CAN/ETH总线解析 |
| **RSP MIL Python** | ✅ 完成 | 完整 RSP 流水线: DC去除+干扰抑制+Range FFT+Doppler FFT+DDMA+CFAR+DOA | — |
| **RSP Cuda** | ✅ 完成 | GPU 加速 RSP 流水线: cuFFT+峰值检测+批量DOA | — |
| **3D Object Detection** | ⚠️ 部分 | 欧氏聚类(真实) + 速度字段(待实现) | AI模型部署 + 速度/角度估计 |
| **Rviz_radar** | ✅ 完成 | 透传DetList/ObjList可视化 | - |
| **Rviz_image** | ✅ 完成 | 透传Image叠加信息 | - |
| **Rviz_ruler** | ✅ 完成 | 静态参考标记 | - |
| **Logging** | ✅ 完成 | 透传记录所有通道 | - |

### 已清除的模拟代码

以下模拟数据生成代码已全部移除（2026-06-30），替换为真实算法或默认值：

| 文件 | 清除内容 | 替代行为 |
|------|---------|---------|
| `rsp_mil_python.py` | `np.random.uniform()` 随机检测点生成 (30点/帧) | 完整 RSP 流水线: Range/Doppler FFT + CFAR + DOA → 真实 DetList |
| `rsp_cuda.py` | `np.random.uniform()` 随机检测点生成 (45点/帧) | GPU 加速 RSP 流水线 → 真实 DetList |
| `vehicle_data_rx.py` | `np.random.normal()` 高斯随机车速/加速度 | 发布默认值 + is_default=True |
| `vehicle_data_rx.cpp` | Gaussian随机 + heading积分 + 随机噪声 | 发布默认值 + is_default=True |
| `object_detection_3d.py` | `np.random.uniform()` 随机vx/vy速度 | 速度字段设为0.0 + TODO标记 |
| `adc_rx_analog.cpp` | 单文件错误加载 (已修复为双文件合并) | ctrx0+ctrx1逐帧拼接, 噪声池仅备选 |

### 下一步开发优先级

1. **🔴 P0**: 实现 RSP Python 版真实信号处理算法（Range-FFT → Doppler-FFT → CFAR → 角度估计）
2. **🟡 P1**: 接入 CAN/ETH 总线，替换车辆数据默认值
3. **🟢 P2**: 实现 CUDA 加速版 RSP
4. **🟢 P2**: 部署 AI 3D 目标检测模型
5. ~~**P1**: 接入真实相机驱动~~ ✅ 已完成 (Rmoncam A2 1080P USB UVC, 2026.7.1)

---

## 十二、性能优化经验总结（2026-07-02）

> 本章记录从 CycloneDDS 0.7.0 切换到 FastDDS 2.1.4 的完整经验，涵盖 DDS 传输层、Python 异步 I/O、消息字段溢出等关键问题的根因分析与解决方案。

### 12.1 核心问题：DDS 传输层选型错误

#### 现象

使用 `bash scripts/start.sh python --analog` 启动框架后：
- `ego_motion.csv` 正常写入 1000+ 行（小消息 ~100B）
- `adc_data/` 和 `camera_front_center/` **无任何文件**（大消息 32MB / 200KB）
- 终端持续报错: `ddsi_udp_conn_write to udp/192.168.137.181 failed with retcode -58`
- `ros2 topic echo` 也收不到任何数据

#### 根因分析

| 层次 | 问题 | 影响 |
|------|------|------|
| **RMW 层** | CycloneDDS 0.7.0 (ros-foxy-cyclonedds) **不支持 Shared Memory 传输**（SHM 在 0.9.0+ 引入） | 所有 DDS 流量强制走 UDP socket |
| **网络层** | CycloneDDS 默认绑定 WiFi 网卡 (`wlan0`, IP 192.168.137.181)，而非本地回环 | 32MB 消息被发送到 WiFi 网卡 |
| **协议层** | UDP MTU 1500 字节，32MB ADC 帧被切割成 **~23,000 个 IP 分片** | 任一分片丢失 = 整帧丢弃 |
| **QoS 层** | Best Effort 模式下无重传机制 | 分片丢失不可恢复 |

```
错误的数据路径（CycloneDDS 0.7.0）:
  adc_rx (C++) → UDP socket → wlan0 → 192.168.137.181 → 23,000 分片 → 大量丢包 → logging_node 收不到

正确的数据路径（FastDDS）:
  adc_rx (C++) → SHM (/dev/shm) → 零拷贝 → logging_node 直接读取
```

#### 解决方案：切换到 FastDDS

```bash
# env.sh: 不再强制 CycloneDDS，默认使用 ROS2 Foxy 自带的 FastDDS
# FastDDS 2.1.4 内置 SHM 传输，自动检测同机节点并走共享内存
```

| 对比维度 | CycloneDDS 0.7.0 | FastDDS 2.1.4 |
|---------|:---:|:---:|
| SHM 支持 | ❌ 不支持 (0.9.0+ 才有) | ✅ 内置，默认启用 |
| 32MB 消息传输 | ~23,000 UDP 分片 | 1 个 SHM buffer，零拷贝 |
| 同机延迟 | 网络栈往返 ~1-5ms | 内存拷贝 ~0.1ms |
| 跨机通信 | ✅ 原生 UDP 多播 | ✅ 保留内置 UDP 回退 |
| 配置复杂度 | 需 XML 调优 MaxMessageSize | 默认即可，可选 XML 调参 |

#### 关键配置文件

**`config/fastdds.xml`** — 128MB 共享内存段，确保 32MB ADC + 5MB RnNci 同时传输：

```xml
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>ft_shm_transport</transport_id>
      <type>SHM</type>
      <segment_size>134217728</segment_size>  <!-- 128MB -->
    </transport_descriptor>
  </transport_descriptors>
  <participant profile_name="ft_default_participant" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>ft_shm_transport</transport_id>
      </userTransports>
      <useBuiltinTransports>true</useBuiltinTransports>  <!-- 保留 UDP 跨机回退 -->
    </rtps>
  </participant>
</profiles>
```

> **教训**: FastDDS 2.1.x SHM transport descriptor 仅支持 `transport_id`、`type`、`segment_size` 三个元素。`max_message_size`、`healthy_check_timeout_ms`、`port_queue_capacity` 等字段在 2.1.4 中**不存在**，会导致 XML 解析失败 → 自定义 SHM 被跳过 → 回退默认 512KB 段。

### 12.2 Python GIL 与异步 I/O 优化

#### 问题

`logging_node` 的 ROS2 回调在 Python 主线程中执行（受 GIL 限制），以下操作直接阻塞所有回调：

| 操作 | 耗时 | 影响 |
|------|------|------|
| `bytes(msg.data)` 32MB 拷贝 | ~5-10ms | 阻塞 ADC/Image/Det 回调 |
| `cv2.imencode('.jpg')` | ~10-30ms | 阻塞所有回调 |
| `os.remove()` 删除文件 | ~1-5ms | 偶发阻塞 |
| 每 50Hz ego 全量重写 CSV | ~2-5ms | 50 次/秒磁盘 I/O |

#### 解决方案

| 优化 | 实现 | 收益 |
|------|------|------|
| **ADC 零拷贝** | `struct.pack` 20B header + `f.write(array.array)` buffer protocol，入队到独立写线程 | 消除 32MB `bytes()` 拷贝 |
| **Image 异步编码** | `cv_bridge.imgmsg_to_cv2()` + `cv2.imencode()` 卸载到 AsyncWriter 线程 | 回调中仅入队引用，<0.1ms |
| **ego 批量刷新** | 内存追加行 → 每 50 条或 1s 定时触发一次文件重写 | 50Hz → ~1Hz 磁盘写入 |
| **异步文件删除** | `os.remove()` 入队到写线程 | 回调不阻塞 |
| **队列背压控制** | `Queue(maxsize=500)`，满时丢弃最旧 | 防止内存无限增长 |

### 12.3 DDS QoS 可靠性匹配

#### 问题

`logging_node` 对小消息（DetList、ObjList、Ego）使用 **Reliable** QoS，但 C++ rx 节点全部使用 **Best Effort**。DDS 规范要求 publisher 和 subscriber 的 reliability 必须一致，否则**无法建立匹配**。

**现象**: `ros2 topic info` 显示 subscription count = 0，消息永远不会被投递。

#### 解决

统一使用 Best Effort QoS。同机 SHM 传输下 Best Effort 实际等价于 Reliable（无网络丢包）。

### 12.4 消息字段 uint32 溢出

#### 问题

`RnNciData.frame_timestamp_us` 字段定义为 `uint32`（最大值 4,294,967,295），但 Unix 微秒时间戳（如 `1782974334 × 1,000,000 = 1,782,974,334,000,000`）远超此范围。

**现象**: `rsp_mil_python._publish_rn_nci` 每帧都因 `AssertionError: value must be in [0, 4294967295]` 崩溃。

**连锁反应**: `_publish_rn_nci` 在 `_publish_det_list` **之前**调用 → 异常导致 `return` → **DetList 永不发布** → 整个下游管线（点云 PCD/CSV、3D 目标检测、obj_list）全部为空。

#### 解决

1. **调换执行顺序**: `_publish_det_list` 先于 `_publish_rn_nci`，且 `_publish_rn_nci` 用独立 `try/except` 包裹
2. **截断时间戳**: `frame_timestamp_us = (timestamp_us) & 0xFFFFFFFF`，完整时间戳在 `header.stamp` 中保留

### 12.5 经验总结

| 经验 | 说明 |
|------|------|
| **先确认 RMW 版本和能力** | CycloneDDS 0.7.0 ≠ 0.10.0，SHM 支持是分水岭 |
| **同机大消息必须走 SHM** | UDP 分片不可靠，32MB → 23000 分片，任一丢失整帧报废 |
| **FastDDS 是 ROS2 Foxy 默认** | 内置 SHM，零配置即可工作，跨机时自动回退 UDP |
| **Python ROS2 回调串行执行** | GIL 限制，重操作（编码、拷贝、I/O）必须卸到独立线程 |
| **QoS 匹配是硬约束** | publisher/subscriber reliability 必须一致，否则静默丢弃 |
| **字段类型要匹配实际数据范围** | Unix 微秒时间戳需 uint64，uint32 仅能容纳 ~71 分钟 |
| **异常处理要考虑级联效应** | 一个函数崩溃不能阻断整个 pipeline |
| **XML 配置要验证版本兼容性** | 不同版本的 DDS 支持的 XML 元素不同，非法元素导致静默回退 |